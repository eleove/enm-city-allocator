# this command launches the front server (on port 3001), API calls need to be made on port 3000
web: cd client && yarn start 

# this command launches the Rails server
api: bundle exec rails s -p 3000

# there has to be 2 different ports otherwhise there would be routes conflits
